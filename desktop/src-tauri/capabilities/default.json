{
  "identifier": "default",
  "description": "Default capability for CP IDE",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "fs:default",
    "dialog:default"
  ]
}
